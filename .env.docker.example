# Course Notifier - Docker Environment Configuration
# 
# Copy this file to .env.docker and fill in your values:
#   cp .env.docker.example .env.docker
#
# Then run:
#   docker compose --env-file .env.docker up -d
#
# ⚠️  NEVER commit .env.docker to version control!

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
POSTGRES_USER=coursenotifier
POSTGRES_PASSWORD=your-secure-database-password-here
POSTGRES_DB=coursenotifier

# ===========================================
# AUTHENTICATION
# ===========================================
# Generate a secure random string (e.g., using: openssl rand -hex 32)
JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production
JWT_EXPIRES_IN=7d

# ===========================================
# EMAIL CONFIGURATION (Gmail Example)
# ===========================================
# For Gmail, you need to use an "App Password":
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Generate a new app password for "Mail"
# 3. Use that 16-character password here

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=your-email@gmail.com

# Optional: Backup SMTP (fallback if primary fails)
SMTP_BACKUP_USER=
SMTP_BACKUP_PASS=
SMTP_BACKUP_FROM=

# ===========================================
# APPLICATION URLs
# ===========================================
# For local development:
CLIENT_URL=http://localhost:3000
VITE_API_URL=http://localhost:5000

# For production (example):
# CLIENT_URL=https://coursenotifier.yourdomain.com
# VITE_API_URL=https://api.coursenotifier.yourdomain.com

# ===========================================
# PORTS (Optional - defaults shown)
# ===========================================
SERVER_PORT=5000
CLIENT_PORT=3000

# ===========================================
# SCRAPER SETTINGS
# ===========================================
SCRAPER_AUTO_SYNC=true
SCRAPER_INTERVAL_MINUTES=60

# ===========================================
# MONITORING (Optional)
# ===========================================
# Sentry DSN for error tracking
# Get yours at https://sentry.io
SENTRY_DSN=
VITE_SENTRY_DSN=

# ===========================================
# NODE.JS SETTINGS
# ===========================================
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=512
